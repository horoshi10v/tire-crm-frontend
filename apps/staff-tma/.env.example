# API URL for the Go Backend
VITE_API_BASE_URL=http://localhost:8083/api/v1

# Dev-only Vite proxy target. This value is read by vite.config.ts
# and is not exposed to the browser bundle.
DEV_PROXY_TARGET=http://localhost:8083
